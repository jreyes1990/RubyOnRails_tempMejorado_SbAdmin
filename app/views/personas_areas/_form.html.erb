<%= form_with(model: personas_area, local: true) do |form| %>
  <% if personas_area.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(personas_area.errors.count, "error") %> prohibited this personas_area from being saved:</h2>

      <ul>
        <% personas_area.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_created_id %>
    <%= form.number_field :user_created_id %>
  </div>

  <div class="field">
    <%= form.label :user_updated_id %>
    <%= form.number_field :user_updated_id %>
  </div>

  <div class="field">
    <%= form.label :estado %>
    <%= form.text_field :estado %>
  </div>

  <div class="field">
    <%= form.label :area_id %>
    <%= form.text_field :area_id %>
  </div>

  <div class="field">
    <%= form.label :persona_id %>
    <%= form.text_field :persona_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
